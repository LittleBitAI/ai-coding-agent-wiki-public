# 위키 구조

공용 규칙과 프로젝트 지식은 서로 다른 축이다.

| 축 | 내용 | 위치 | 전달 |
| --- | --- | --- | --- |
| rule | 공용 정책, 프로젝트별 규칙 | `operator/`, `craft/`, 대상 `.wiki/*.md` | 발화 트리거 |
| repo | 문서 목록, 현황, 결정 | 대상 프로젝트 문서와 `.wiki/decisions/` | 세션 시작 또는 결정 트리거 |

프로젝트 `.wiki/*.md`의 규칙도 rule 예산에 들어간다. 위치만으로 축을 판단하지 않는다.
규칙에서 프로젝트 문서를 가리키는 `reads` 참조는 허용한다. 두 축의 비용은 따로 센다.
임베딩 검색은 사용하지 않는다. 규칙 선택에는 트리거와 실패 이름을 사용한다.

## 규칙 파일

```yaml
---
scope: craft
severity: contract
triggers: ['검증']
slots: []
sources: []
links: [verify-narrow-then-wide]
---
```

본문은 제목과 `규칙.` 문장으로 시작한다. `[[페이지-이름]]` 또는 `links`로 관련 규칙을 연결한다.
`triggers`는 정규식 목록이다. `contract`와 `landmine`은 조건에 맞을 때 주입된다.
`landmine`에는 확인 가능한 근거가 필요하다. 이 공개본은 개인 사례를 포함하지 않으므로
기본 규칙을 일반 계약인 `contract`로 제공하며 경험적 검증을 했다고 주장하지 않는다.
`sources`에는 공개 가능한 근거 파일만 넣는다. 비공개 발화나 코드 인용을 넣지 않는다.

`{gate_cmd}` 같은 슬롯은 대상 프로젝트 `.wiki/adapter.toml`의 `[slots]`에서 읽는다.
프로젝트 폴더명이 같아도 각 checkout의 값을 따로 사용한다.
`enforce.deny`는 차단할 도구나 명령, `enforce.pretooluse`는 검사 스크립트 이름이다.

## 새 페이지를 넣는 기준

기존 페이지 수정으로 표현할 수 있으면 새 페이지를 늘리지 않는다.
공유할 규칙에는 적용 조건, 행동, 예외를 적고, 프로젝트 고유 값은 대상 adapter에 둔다.
공개본의 가상 사례를 실제 사고 근거처럼 제시하지 않는다.

## 로컬 기록

`raw/`의 대화·분석 자료와 `.wiki/`의 생성 기록은 기본적으로 공개하지 않는다.
`graph.json`은 공용 규칙 지도, 대상 `.wiki/graph.json`은 프로젝트 문서 지도다.
지도에 개인 프로젝트를 넣으면 경로와 현황도 포함될 수 있으므로 생성 결과를 검토 없이 공유하지 않는다.
`tool/lint.py --check`와 `tool/repo_lint.py`는 각각 공용 규칙과 대상 프로젝트를 검사한다.
