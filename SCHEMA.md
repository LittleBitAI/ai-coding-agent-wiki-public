# 스키마 — 이 위키가 무엇이고 어떻게 생겼는가

이 파일이 규약이다. 페이지를 쓰거나 고치기 전에 여기를 읽는다.

그러므로 병목은 저장도 검색도 아니다. 적힌 것이 행동을 지배하지 못하는 것이다.

이 위키의 값어치는 둘뿐이다.

1. 반복 지시를 관습으로 굳힌다 — 지시문을 스킬 하나로.
2. 가능한 규칙을 검사로 바꾼다 — 실제로 지켜진 규칙들은 산문이 아니라
   던지는 가드였고 빨개지는 게이트였다.

나머지는 문장으로 남는다. 판단이 필요한 것을 기계화하면 거짓 양성이 작업을 멈춘다.

## 세 층 (Karpathy 명세의 각색)

| 층 | 누가 소유 | 여기서는 |
| --- | --- | --- |
| Raw Sources | 사람 | `raw/` — 세션 로그·커밋·라이브 아카이브에서 뽑은 불변 측정 결과 |
| Wiki | 에이전트 | `operator/` `craft/` — 페이지. 링크로 엮고 모순은 표시한다 |
| Schema | 사람 ↔ 에이전트 | 이 파일 |

원안과 다른 점 하나. Karpathy 의 원시 소스는 논문·기사 같은 외부 문서이지만,
여기서는 대화 로그와 실행 기록이다. 그래서 ingest 가 "읽고 요약"이 아니라
"세고 대조"다.

## 범위 — 무엇을 어디에 두는가

이것이 이 위키의 가장 중요한 결정이다. 프로젝트 A 의 지뢰가 프로젝트 B 로
새면 그 자체가 사고다. 반대로 아무것도 공유하지 않으면 같은 교훈을 프로젝트마다
다시 가르쳐야 한다.

| 범위 | 따라다니는 것 | 예 |
| --- | --- | --- |
| `operator` | 사람 | 리뷰 루프 프로토콜 · 질문은 선택지로 · 파일 수정은 diff 로 · 회차마다 보고 |
| `craft` | 에이전트의 기술 | 재현 먼저 · 초록 신호를 의심하라 · 한 증인은 한 판단만 · 죽은 행은 삭제 전에 증명 |
| `project` | 저장소 | 게이트 명령 · 런처 · 포트 · 아키텍처 불변 |

`project` 범위의 페이지는 이 저장소에 없다. 대상 저장소의 `.wiki/` 에 살고,
훅이 거기까지 읽는다. 남의 지뢰를 여기 들이지 않는다.

### 손으로 쓴 페이지는 몇 장뿐이다 — 나머지는 목록이 든다

`corpus` 가 문서 전부의 목록을 만들고, 세션 시작에 그것을 한 번 싣는다. 본문은 안 싣는다. 에이전트는 파일을 열 수 있다.

고르는 것은 기계가 안 한다.

규칙과 문서를 갈라야 이게 성립한다. 규칙은 훅이 주입한다 — 훅이 모델보다
먼저 돌고, 규칙의 실패는 "안 읽었다" 라서 고르게 두면 안 된다. 문서의 실패는
"있는 줄 몰랐다" 이므로 목록이면 족하다. 둘은 다른 문제이고 다른 기계를
쓴다.

### project 범위에도 기계가 붙는다 — 처음엔 안 그랬다

규칙만 사다리에 올리고 지식은 산문에 둔 것이 잘못이었다. 잊는 것에 규칙과
지식의 구별이 없다.

그래서 프로젝트 층도 같은 2층을 쓴다. 다만 얇다 — 권위 문서를 통째로
싣지 않는다. 페이지는 반드시 컨텍스트에
있어야 할 몇 줄과 `reads:` 로 권위 문서를 가리킨다. lint 가 그 경로의 존재를
매번 검사한다.

```markdown
---
scope: project
severity: landmine
triggers: ["왜 (안|못|틀)", "원인", "진단"]
reads: [CLAUDE.md, docs/architecture/pipeline.md]
---
```

### 슬롯 — 형태는 공유하고 값은 프로젝트가 채운다

형태가 같고 값이 다르다. 값을 공유하면 둘 다 깨지고, 아무것도 공유하지 않으면
같은 교훈을 두 번 배운다. 그래서 페이지가 빈칸을 선언하고 어댑터가 채운다.

```markdown
---
scope: craft
slots: [env_name, activate_cmd, verify_cmd]
---
맨 `python` 을 믿지 마라. 환경을 이름으로 지정하라.
확인: `{activate_cmd}` 뒤에 `{verify_cmd}` 가 통해야 한다.
```

슬롯 값은 대상 checkout의 `.wiki/adapter.toml`에 산다. 허브에 복사하지 않으며 페이지에는 절대 안 적는다.
로컬 adapter가 없는 기존 설치만 허브 `adapters/<project>.toml`을 사용한다.
팀 설치 도구가 고른 호스트는 대상의 Git 제외 파일 `.wiki/installed-agents.json`에 남긴다.
배선 검사는 이 PC별 기대값을 우선하고, 기록이 없으면 adapter의 `agents`를 따른다.

## 페이지의 최소 구조

```markdown
---
scope: operator | craft
severity: landmine | contract | preference
triggers: ["정규식", ...]     # 이 발화를 보면 주입한다. landmine/contract 는 필수
slots: []                    # 없으면 빈 목록
enforce:                     # 검사로 옮길 수 있으면. 없으면 생략
  deny: ["Bash(sed -i*)"]    #   1층. apply 가 settings.json 에 합친다
  pretooluse: <tool/의 스크립트>  #   4층. 계산이 필요한 판정일 때만
sources: [raw/xxx.md#L12]    # 이 페이지가 어디서 나왔는가
links: [other-page]
---

# 제목 — 한 줄로 무엇을 말하는가

규칙. 한 문단. 명령형으로.

왜. 무엇이 실제로 터졌는가. 재현 가능한 형태로.

어겼을 때. 무슨 일이 일어나는가.
```

### severity 는 취향이 아니라 대가다

| 등급 | 기준 | 어디에 실리나 |
| --- | --- | --- |
| `landmine` | 어겨서 실제로 무언가를 태웠다 (라이브 회차·시간·데이터) | 주입 자격 있음 |
| `contract` | 어기면 제품이 틀린 답을 낸다 | 주입 자격 있음 |
| `preference` | 취향·형식. 어겨도 제품은 맞다 | index 로 내림 |

"항상 로드" 가 아니다. 등급은 *주입될 자격*을 줄 뿐이고, 실제로 전문이
실리는 것은 그 발화가 `triggers` 에 걸렸을 때다. `landmine` 이어도 안 걸리면
안 실린다 — 그래서 `triggers` 가 `landmine`·`contract` 에 필수다.

예외가 하나 있고 그것도 트리거로 적혀 있다. `operator/agent-delegation` 은
`triggers` 가 비어 있지 않은 모든 발화에 걸리게 쓰여 있다.

`SessionStart` 가 싣는 것은 이것과 다른 경로다 — 문서 목록과 계획과 최근
결정 요약이고, 페이지 전문이 아니다. 둘을 같은 것으로 세면 비용도 적중률도 틀린다.

`landmine` 은 근거 없이 못 붙인다. `sources` 에 그것이 무엇을 태웠는지가
있어야 한다.

### 페이지가 되는 문은 둘이다

| 문 | 근거 | 예 |
| --- | --- | --- |
| 반복 | 둘 이상의 저장소에서 사용자가 다시 쳤다 | 리뷰 루프 · 머지 후 정리 |
| 강제 | 한 곳에서라도 이미 검사·차단으로 서 있고 옮길 수 있다 | `git reset --hard` 차단 |

둘째 문이 없으면 1층에 실을 것이 영원히 안 생긴다. census 는 사용자가 다시 친
것을 세는데, 이미 차단된 규칙은 재입력을 안 만들기 때문이다. 작동하는 강제는
자기 흔적을 지운다.

## 모순은 잘못이 아닐 수 있다

두 페이지가 다른 말을 하면 셋 중 하나다. 어느 것인지 가르는 것이 lint 의 일이고,
**가르기 전에 고치면 정보를 지운다.**

| 무엇 | 뜻 | 처리 |
| --- | --- | --- |
| 드리프트 | 같은 질문에 두 답. 하나가 낡았거나 틀렸다 | 고친다 |
| 의도된 갈림 | 다른 질문인데 닮아 보인다 | 고치지 말고 기록한다 |
| 미해결 | 어느 쪽이 맞는지 아직 모른다 | 둘 다 두고 모른다고 적는다 |

그래서 페이지가 갈림을 선언한다. 선언된 갈림은 lint 가 다시 안 짚는다.

```yaml
---
conflicts_with:
  - page: craft/other-page
    kind: intended        # intended | unresolved
    why: 두 표가 다른 질문에 답한다.
    measured: raw/census-x.jsonl
---
```

`kind: unresolved` 는 아직 모른다는 선언이다. 그것도 기록이다 — 모르는 것을
아는 척 통일하는 것보다 낫다.

선언 없이 어긋난 값만 lint 가 짚는다. 선언은 사람이 판단해 쓴다.

## 워크플로 셋

| 이름 | 무엇 | 언제 |
| --- | --- | --- |
| `census` | 대상 저장소의 세션 로그를 세어 무엇이 실제로 고장 나는지 낸다 | 새 프로젝트를 붙일 때 맨 처음 |
| `harvest` | PR 본문에서 "무엇을 왜 골랐나" 를 캐 `.wiki/decisions/` 로 만든다 | 프로젝트를 붙일 때 한 번 |
| `corpus` | 저장소 문서 전부의 목록을 만든다 | 문서가 바뀔 때마다 |
| `repo_graph` | 그 목록 위에 링크를 얹어 아무도 안 가리키는 문서를 센다 | 목록이 바뀔 때. `sync` 가 부른다 |
| `sync` | 목록·결정·검진을 저장소 상태에 맞춘다 | 자동. `Stop` 훅이 부른다 |
| `apply` | 페이지의 `enforce` 와 훅 셋을 대상의 `.claude/settings.json` 에 합친다 | 페이지나 슬롯이 바뀔 때마다 |
| `trigger_audit` | 트리거 적중과 **한 턴에 실리는 양**을 잰다 | 페이지나 트리거를 더한 뒤 |
| `lint` | 허브 위키 — 끊어진 링크·고아·낡은 서술·선언 안 된 모순·빠진 연결·근거 없는 `landmine` | 머지 후 · `apply` 전 · 페이지를 고친 뒤 |
| `repo_lint` | 붙은 저장소 — 낡은 목록·끊어진 포인터·안 실리는 규칙 | 자동. `Stop` 훅이 `sync` 로 부른다 |
| `graph` | 정책 그래프를 `graph.json` 으로 낸다. 그리는 것은 `web/` 이 한다 | 페이지가 바뀔 때마다 |

`lint` 가 선택이 아니라 본편이다.
유지되지 않는 위키는 낡은 위키보다 나쁘다 — 틀린 것을 자신 있게 말하기 때문이다.

언제 돌리고 발견을 어떻게 처리하는지, 그리고 새로 생긴 것이 어느 층으로
가는지는 [`MAINTENANCE.md`](MAINTENANCE.md) 가 갖는다.

## 안 하는 것

- 임베딩도 벡터 DB 도 쓰지 않는다. 읽는 쪽이
  이미 좋은 모델인데 그 앞에 검색 모델을 하나 더 세우는 것은 우아하지 않고,
  키·네트워크·할당량·낡는 벡터라는 실패 자리를 넷 더 만든다. 목록이면 된다.
- 대상 저장소의 지식을 여기로 가져오지 않는다. `project` 범위는 그쪽에 산다.
- 판단이 필요한 것을 기계화하지 않는다. 거짓 양성이 작업을 멈춘다.
- 읽히지 않을 페이지를 쓰지 않는다. 페이지가 늘면 `apply` 가 싣는 양이 늘고,
  그것이 처음에 이 위키를 만들게 한 바로 그 문제다.

## 공개 사본의 근거 기록

원본의 규칙·스킬·적용 조건·등급은 유지하고 실제 대화·사례·측정 기록만 제외했다.
`sources_withheld: true`는 원본 근거가 비공개임을 나타낸다. 이 표시가 있는 기존 규칙에만
lint의 근거 누락 예외를 적용하며, 새 규칙의 근거를 대신하지 않는다.
설치와 본인 CLI 로그인은 [설치 안내](docs/chat-setup.md), 공개본 관리는
[갱신 안내](docs/publishing.md), 검증 범위는 [검사 기록](docs/verification.md)을 따른다.
