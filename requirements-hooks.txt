# hooks 설치 하나가 필요로 하는 전부. 채팅과 개발 요구사항이 이것을 포함하므로
# 여기 한 줄을 더하면 세 설치 경로가 같이 받는다.
#
# 손으로 적힌 목록이 네 곳에 흩어져 있었다 — README, hooks 설치 안내,
# setup_agents 의 확인, 그리고 요구사항 파일. 새 의존성 하나가 그중 하나에만
# 들어가면 문서대로 설치한 기계에서 그 훅만 조용히 안 돈다. 실제로 그렇게 됐다.
#
# markdown-it-py 는 extra 없이 쓴다. `[linkify]` 가 필요한 프리셋을 쓰면
# 없는 기계에서 parse 가 던지고 훅이 그것을 삼킨다 — craft/emphasis-is-scarce.
PyYAML==6.0.3
markdown-it-py>=2,<5
