---
colors:
  ground: "#f5f4f1"
  ink: "#16181d"
  panel: "#ffffff"
  chip: "#ecebe6"
  rule: "#e6e3dd"
  edge: "#d9d6d0"
  ink-soft: "#4a5058"
  ink-faint: "#878d95"
  ok: "#3f6b8f"
  warn: "#a83a2c"
  add: "#2f6b4f"
  del: "#a83a2c"
  dark:
    ground: "#14161a"
    ink: "#eceae5"
    panel: "#1c1f25"
    chip: "#232830"
    rule: "#262b32"
    edge: "#2e333b"
    ink-soft: "#a8aeb6"
    ink-faint: "#6f757d"
    ok: "#7fa9c9"
    warn: "#d9705e"
    add: "#7fbf9a"
    del: "#d9705e"
  mirror:
    self: "#3f6b8f"
    self-dark: "#7fa9c9"
typography:
  sans: '"IBM Plex Sans KR", system-ui, sans-serif'
  heading: '"IBM Plex Sans Condensed", "IBM Plex Sans KR", system-ui, sans-serif'
  mono: '"IBM Plex Mono", ui-monospace, monospace'
  scale:
    said: "1rem / 1.8 / 400"
    self: "1rem / 1.8 / 600"
    did: "0.8125rem / 1.5 / 400"
    meta: "0.75rem / 1.4 / 500 / 0.02em"
spacing:
  unit: "0.25rem"
  steps: [2, 4, 6, 8, 12, 16, 20, 24, 32]
  gutter: "20px"
  prose: "40rem"
  time: "3.25rem"
  mark: "1.5rem"
  split: "72rem"
rounded:
  radius: "0.375rem"
  sm: "0.225rem"
  lg: "0.525rem"
components:
  - 상태 바 — 호스트 · 세션 파일 · 연결 표시
  - 대화 면 — 사람 발화 · 에이전트 산문 · 도구 설명 · 세션 구분선
  - 명령 면 — 도구 이름표 · 명령 · 패치
  - 빈 상태 (면마다 하나)
---

# DESIGN — 이 저장소의 화면 값

값을 여기서 처음 정한 것이 아니다. `web/src/index.css`와 지도 뷰가 이미 쓰고
있던 토큰을 옮겨 적었다. 화면이 둘 이상이 된 순간부터 "그 파랑이 어느
파랑인가"를 대조할 자리가 필요해졌고, 그 자리가 이 파일이다.

## 색

중립 바탕에 파랑 하나. `ok`만 채도가 있고 나머지는 전부 회색 계열이다. 읽는
화면이라 색이 말을 걸면 안 된다 — 강조는 색이 아니라 굵기와 여백이 한다.

`warn`은 실패에만 쓴다. 연결이 끊긴 것은 실패가 아니라 상태이므로 `ink-faint`다.

`mirror.self`는 사람이 친 말을 에이전트의 말과 가르는 유일한 색이다. 미러는
이 구분 하나만 색으로 하고, 나머지 층위는 크기와 농도로 가른다.

`add`·`del`은 패치의 `+`·`-` 줄에만 쓴다. 이 둘은 상태가 아니라 방향이므로
`ok`·`warn`과 같은 값을 쓰지 않는다 — 지워진 줄은 오류가 아니다.

## 타이포그래피

단계가 넷이다. 세어진다.

| 단계 | 어디 | 값 |
| --- | --- | --- |
| said | 에이전트 산문. 화면의 기본 | 1rem / 1.8 / 400 |
| self | 사람이 친 말 | 1rem / 1.8 / 500 + `mirror.self` |
| did | 도구 한 줄 설명 | 0.8125rem / 1.7 / `ink-faint` |
| meta | 시각 거터 · 표지 · 면 이름표 · 도구 이름 | 0.6875rem / 1.6 / 400·600 |

`said`와 `self`는 크기가 같다. 같은 대화의 두 목소리라 크기로 가르면 한쪽이
덜 중요해 보인다 — 가르는 것은 굵기와 색이다.

`meta`는 행 종류마다 다른 크기의 본문 첫 줄에 맞춰 서야 한다. 그 정렬을
`line-height`로 하면 한 단계가 네 단계로 불어난다 — 실제로 그렇게 됐고 잰
값이 8단계였다. 정렬은 `padding-top`이 하고 단계는 하나로 둔다.

줄길이는 `40rem`. 한국어 산문이 한 줄에 30자 안쪽으로 떨어지는 폭이다.

## 간격

2px 배수만 쓴다. `did`는 연달아 나오므로 위아래를 좁혀 묶어 보이게 하고,
`said`와 `self`는 떼어 turn 이 갈리게 한다. 묶는 것도 가르는 것도 여백이 한다.

## 화면 목록

미러는 화면이 하나고, 그 안이 두 면이다. 대시보드도 상세 페이지도 없다.

```
누가:        영어로 말하는 에이전트 옆에 앉은 한국어 사용자
무엇을 하러: 지금 저 셀에서 벌어지는 일을 한국어로 읽으러
성공:        작업 셀에서 방금 나온 말이 몇 초 안에 여기 한국어로 뜬다
```

읽으러 오는 사람에게 필요한 것은 흐름과, 그 흐름이 지금 살아 있다는 표시다.
그 둘 말고는 안 놓는다.

## 왜 한 열이 아니라 두 면인가

산문과 코드는 필요한 폭이 정반대다. 한국어 산문은 measure 가 없으면 안 읽히고
명령·패치는 접히면 안 읽힌다. 한 열에 같이 두면 둘 다 진다 — 폭을 산문에 맞추면
1920px 화면에서 가운데 한 줄만 쓰고 양옆 1100px 가 빈다. 그게 실제로 나온 화면이었다.

그래서 왼쪽은 `40rem` 로 묶은 대화 면, 오른쪽은 안 묶은 명령 면이다. 넓은 화면이
버는 자리는 전부 코드가 쓴다. 두 면은 각자 스크롤하고, 각자 자기 바닥에 붙는다 —
읽는 사람이 이미 바닥에 있을 때만.

접는 폭은 `72rem`다. 기기 규격이 아니라 두 면이 더는 나란히 안 서는 폭이다.

## 화면을 안 끌어가는 규칙

새 줄이 와도 스크롤을 옮기지 않는다. 옮기는 경우는 하나 — 그 면의 바닥을 이미
읽고 있었을 때. 따라가기 토글도 "맨 아래로" 알림도 없다. "지금 바닥인가" 가
같은 질문에 이미 답하므로 둘 다 안 만든다.
