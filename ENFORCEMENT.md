# 규칙 적용 방식

규칙을 적는 것과 호스트가 실행하는 것은 별개다. 필요한 위치에 맞는 수단을 고른다.

| 층 | 수단 | 담당 |
| --- | --- | --- |
| 1 | 도구·명령 차단 선언 | Claude permissions 또는 Codex 검사 훅 |
| 2 | 발화에 맞는 규칙 주입 | `tool/inject.py` |
| 3 | 여러 단계의 작업 절차 | `skills/` |
| 4 | 실행 전후 검사 | PreToolUse 및 Stop 훅 |
| 5 | 문서 지침 | 규칙 페이지 본문 |

`tool/apply.py`는 기존 설정에 공용 훅을 병합한다.
SessionStart는 문서 목록과 현황을 전달하고, UserPromptSubmit은 발화에 맞는 규칙을 전달한다.
Stop의 sync는 로컬 지식을 갱신하며 declared_continuation은 실행 약속만 남긴 종료를 검사한다.

Claude의 permissions와 Codex의 검사 훅은 같은 보안 장치가 아니다.
검사 훅은 선언된 직접 명령을 비교하며 임의의 중첩 셸이나 동적 명령까지 막는 완전한 보안 경계가 아니다.
호스트의 승인·샌드박스·프로젝트 신뢰 설정을 대신하지 않는다.

기본 규칙에는 한국어 설명 검사, 패치 방식의 파일 수정, 일부 Git 명령 차단,
비동기 질문 도구 차단이 포함된다. 설치 전에 `operator/`와 `craft/`를 읽고 팀의 요구에 맞는지 확인한다.
상위 지시와 호스트가 허용하는 도구 사용 범위를 우선한다.

코드가 바뀌면 실행 시 새 코드가 읽히지만, 이벤트·명령·차단 선언의 변경은 재설치가 필요하다.
`apply --check`는 설치된 설정과 선언을 대조할 뿐 호스트 자동 실행을 증명하지 않는다.
실제 새 세션 확인은 [hooks 안내](docs/hooks-setup.md)를 따른다.
